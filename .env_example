# .env example file for Wikipedia Data Analysis Pipeline
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control - it contains sensitive credentials

# =============================================================================
# SNOWFLAKE CONNECTION (Required)
# =============================================================================
# Your Snowflake account identifier (e.g., 'myorg-myaccount' or 'ab12345.us-east-1')
SNOWFLAKE_ACCOUNT="your_account_identifier"

# Your Snowflake username
SNOWFLAKE_USER="your_username"

# Your Snowflake password
# WARNING: For production, use secure secret management services instead
SNOWFLAKE_PASSWORD="your_password"

# Your Snowflake warehouse (for compute)
SNOWFLAKE_WAREHOUSE="your_warehouse"

# Your Snowflake database
SNOWFLAKE_DATABASE="your_database"

# Schema where all objects will be created (recommended: WIKIPEDIA)
SNOWFLAKE_SCHEMA="WIKIPEDIA"

# Optional: Snowflake role (if not specified, uses your default role)
SNOWFLAKE_ROLE="your_role"

# =============================================================================
# DATA INGESTION SETTINGS
# =============================================================================

# Local directory to store downloaded pageview files
# If not set, defaults to "wikipedia_pageviews_files"
LOCAL_DATA_DIR="wikipedia_pageviews_files"

# Number of parallel workers for downloading files
# Adjust based on your network bandwidth and CPU (default: 5)
MAX_DOWNLOAD_WORKERS=5

# =============================================================================
# SNOWFLAKE OBJECT NAMES (Optional - defaults provided)
# =============================================================================
# Override default Snowflake object names if needed
# SNOWFLAKE_STAGE_NAME="WIKIPEDIA_PAGEVIEWS_STAGE"
# SNOWFLAKE_TABLE_NAME="RAW_WIKIPEDIA_PAGEVIEWS"

# =============================================================================
# SECURITY NOTES
# =============================================================================
# - Never commit the .env file to version control
# - For production environments, use:
#   * AWS Secrets Manager
#   * Azure Key Vault  
#   * Google Secret Manager
#   * HashiCorp Vault
#   * Environment variables in your deployment platform
# - Regularly rotate your Snowflake credentials
# - Use least-privilege access for your Snowflake role

# =============================================================================
# SCHEMA ORGANIZATION
# =============================================================================
# The pipeline creates the following objects in your specified schema:
# - Raw table: {SNOWFLAKE_SCHEMA}.RAW_WIKIPEDIA_PAGEVIEWS
# - Stage: {SNOWFLAKE_SCHEMA}.WIKIPEDIA_PAGEVIEWS_STAGE
# - dbt models: {SNOWFLAKE_SCHEMA}.STG_*, DIM_*, FCT_*, etc.
#
# Using SNOWFLAKE_SCHEMA="WIKIPEDIA" keeps everything organized in one schema